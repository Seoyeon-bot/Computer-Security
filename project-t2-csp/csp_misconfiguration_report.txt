Website: https://facebook.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://instagram.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pinterest.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too lenient; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too lenient; prefetch-src is too strict or missing

Website: https://reddit.com
Misconfigurations: script-src is too lenient; style-src is too lenient; style-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; prefetch-src is too strict or missing

Website: https://tiktok.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://linkedin.com
Misconfigurations: style-src is too lenient; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://quora.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://twitter.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://apple.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too lenient; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://yahoo.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nih.gov
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nytimes.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://yelp.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri uses wildcard, which is overly broad; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://walmart.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://medium.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://theguardian.com
Misconfigurations: script-src is too lenient; style-src is too lenient; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://stackexchange.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cnn.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://usatoday.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://target.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://spotify.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://scribd.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://github.com
Misconfigurations: style-src is too lenient; img-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://bbc.co.uk
Misconfigurations: script-src is too lenient; style-src is too lenient; plugin-types is too strict or missing; sandbox is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://goodhousekeeping.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://clevelandclinic.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://independent.co.uk
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too strict or missing; media-src is too lenient; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://sportskeeda.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://craigslist.org
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri uses wildcard, which is overly broad; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://deviantart.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://moovitapp.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src uses wildcard, which is overly broad; connect-src is too lenient; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://investopedia.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ca.gov
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://waze.com
Misconfigurations: script-src is too lenient; style-src is too lenient; media-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cosmopolitan.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cnbc.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://tumblr.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://amazon.in
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://people.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://www.gov.uk
Misconfigurations: script-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nypost.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://verywellhealth.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://medlineplus.gov
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://steamcommunity.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pbs.org
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://timeout.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://business.site
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://bandcamp.com
Misconfigurations: default-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://expedia.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://telegraph.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://as.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://msn.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; child-src is too strict or missing; connect-src is too lenient; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cbc.ca
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://variety.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ziprecruiter.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mirror.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://depositphotos.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://uol.com.br
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ew.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://steampowered.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://thoughtco.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://theverge.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://usda.gov
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://elle.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://salary.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://msdmanuals.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too lenient; child-src is too strict or missing; connect-src is too lenient; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ubereats.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://infobae.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; frame-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://un.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://wired.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hollywoodreporter.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://economictimes.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://countryliving.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://rollingstone.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://womenshealthmag.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://thespruce.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://who.int
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://techradar.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://menshealth.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://timesofindia.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://everydayhealth.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mit.edu
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://loc.gov
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://lifewire.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://vecteezy.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://allrecipes.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://tenor.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://esquire.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://amazon.co.jp
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://thehindu.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too lenient; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://express.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://kayak.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nextdoor.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://standard.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://billboard.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://statista.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://umich.edu
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://answers.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://tomsguide.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://southernliving.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://kidshealth.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://harpersbazaar.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://medscape.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://lemonde.fr
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://poshmark.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://gamesradar.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://travelocity.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hubspot.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://realsimple.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://foodnetwork.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://commonsensemedia.org
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://huffpost.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://thepioneerwoman.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://airbnb.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://allevents.in
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://archive.org
Misconfigurations: default-src uses wildcard, which is overly broad; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://houzz.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pinterest.es
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too lenient; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too lenient; prefetch-src is too strict or missing

Website: https://goal.com
Misconfigurations: script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; child-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://deadline.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pxfuel.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://jiosaavn.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://betterhealth.vic.gov.au
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://newyorker.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://bhg.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://instructables.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cbssports.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; frame-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://digitalspy.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ctvnews.ca
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://jooble.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://healthdirect.gov.au
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pinterest.de
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too lenient; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too lenient; prefetch-src is too strict or missing

Website: https://vice.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://goodrx.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nike.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mozilla.org
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://byrdie.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://tr-ex.me
Misconfigurations: script-src is too lenient; style-src is too lenient; child-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://drugs.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://delish.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://bustle.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pinterest.fr
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too lenient; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too lenient; prefetch-src is too strict or missing

Website: https://nerdwallet.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://yumpu.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://oracle.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://olx.ua
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; style-src is too lenient; media-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://argos.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://travelandleisure.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://orbitz.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://grubhub.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://glamour.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://vogue.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://health.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://aarp.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://indigo.ca
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://thespruceeats.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://smh.com.au
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://archiveofourown.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://sky.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://skysports.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://justia.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://elconfidencial.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://kohls.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri uses wildcard, which is overly broad; sandbox is too strict or missing; report-to is too strict or missing; manifest-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://foodandwine.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://zdnet.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; frame-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://theathletic.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ieee.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://vk.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://uptodate.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://magazineluiza.com.br
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cntraveler.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://livescience.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://w3schools.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://bol.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://rochester.edu
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://marthastewart.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://amazon.com.au
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://prevention.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ed.gov
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pinterest.co.uk
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action uses wildcard, which is overly broad; frame-ancestors is too lenient; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too lenient; prefetch-src is too strict or missing

Website: https://yandex.ru
Misconfigurations: style-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ultimate-guitar.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://samsclub.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://gq.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://cinemablend.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://fc2.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://housebeautiful.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://peakpx.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://eatingwell.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://architecturaldigest.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://arxiv.org
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://note.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://decider.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://freecodecamp.org
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://allure.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://picclick.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src is too strict or missing; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://vanityfair.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://superuser.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://news.com.au
Misconfigurations: default-src is too strict or missing; script-src is too lenient; style-src is too lenient; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hgtv.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://womansday.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://verywellmind.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://oprahdaily.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nikkei.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://instyle.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://next.co.uk
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://yelp.ca
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri uses wildcard, which is overly broad; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://thriftbooks.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://bonappetit.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://techcrunch.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too strict or missing; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; child-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ea.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://self.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://verywellfit.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://tripsavvy.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://popularmechanics.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://ox.ac.uk
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pcgamer.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hellomagazine.com
Misconfigurations: script-src is too lenient; style-src is too lenient; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://seriouseats.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://diy.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mappy.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://fool.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://smule.com
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://stern.de
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://dickssportinggoods.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src uses wildcard, which is overly broad; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://dzen.ru
Misconfigurations: style-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hola.com
Misconfigurations: script-src is too lenient; style-src is too lenient; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://hilton.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://slate.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://marketscreener.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://wise.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://welt.de
Misconfigurations: script-src is too lenient; style-src is too lenient; font-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://shopee.co.id
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://asda.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://merckmanuals.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too lenient; child-src is too strict or missing; connect-src is too lenient; connect-src uses wildcard, which is overly broad; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://businesswire.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://verizon.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://epicurious.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://shein.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://t-online.de
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://autoblog.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; frame-src is too lenient; frame-src uses wildcard, which is overly broad; media-src uses wildcard, which is overly broad; font-src is too lenient; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; prefetch-src is too strict or missing

Website: https://ihg.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://complex.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://inverse.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://otto.de
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://townandcountrymag.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mathworks.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://mo.gov
Misconfigurations: default-src uses wildcard, which is overly broad; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src is too lenient; img-src uses wildcard, which is overly broad; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too lenient; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://teenvogue.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; connect-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://francebleu.fr
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://voanews.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://seamless.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://brides.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://spiegel.de
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://grammarly.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://engadget.com
Misconfigurations: default-src is too lenient; default-src uses wildcard, which is overly broad; script-src is too strict or missing; style-src is too strict or missing; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src is too strict or missing; font-src uses wildcard, which is overly broad; child-src is too strict or missing; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; prefetch-src is too strict or missing

Website: https://maryland.gov
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://songmeanings.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://liveabout.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://indiewire.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://collegedunia.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://oregon.gov
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://seventeen.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://plex.tv
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nexusmods.com
Misconfigurations: script-src is too lenient; style-src is too lenient; connect-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; prefetch-src is too strict or missing

Website: https://gadgets360.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://theglobeandmail.com
Misconfigurations: default-src is too lenient; script-src is too strict or missing; style-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://journaldesfemmes.fr
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://simplilearn.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://stylecaster.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nzherald.co.nz
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://setlist.fm
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://eltiempo.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://postmates.com
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; img-src is too strict or missing; frame-src uses wildcard, which is overly broad; media-src is too strict or missing; font-src is too strict or missing; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-to is too strict or missing; manifest-src uses wildcard, which is overly broad; prefetch-src is too strict or missing

Website: https://hrs.com
Misconfigurations: default-src is too strict or missing; script-src is too strict or missing; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://nydailynews.com
Misconfigurations: default-src is too lenient; script-src is too lenient; style-src is too lenient; frame-src is too strict or missing; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://pharmeasy.in
Misconfigurations: default-src is too strict or missing; script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too strict or missing; img-src is too strict or missing; frame-src is too strict or missing; media-src is too strict or missing; font-src is too strict or missing; child-src is too strict or missing; connect-src is too strict or missing; object-src is too strict or missing; form-action is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

Website: https://michelin.com
Misconfigurations: script-src is too lenient; script-src uses wildcard, which is overly broad; style-src is too lenient; style-src uses wildcard, which is overly broad; img-src uses wildcard, which is overly broad; frame-src is too strict or missing; media-src uses wildcard, which is overly broad; font-src uses wildcard, which is overly broad; child-src uses wildcard, which is overly broad; connect-src uses wildcard, which is overly broad; object-src uses wildcard, which is overly broad; form-action is too strict or missing; frame-ancestors is too strict or missing; plugin-types is too strict or missing; base-uri is too strict or missing; sandbox is too strict or missing; report-uri is too strict or missing; report-to is too strict or missing; manifest-src is too strict or missing; worker-src is too strict or missing; prefetch-src is too strict or missing

